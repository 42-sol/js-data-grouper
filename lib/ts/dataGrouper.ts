import {
  DataItem,
  GroupingHierarchyLevel
} from './_types';

export function buildDirectoriesByConditions(
  data: DataItem[],
  groupingHierarchy: GroupingHierarchyLevel[]
) {
  console.log(data, groupingHierarchy);

  return [];
}